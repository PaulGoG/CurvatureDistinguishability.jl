# run outputs and logs (never version-controlled)
data/
plots/
*.log
# generated data/figure formats — EXCEPT committed regression fixtures
*.csv
*.h5
*.jld2
*.pdf
*.png
!test/fixtures/**
.DS_Store
docs/build/
# manifests are resolved per host; each run directory keeps a copy of its own
Manifest*.toml
docs/src/generated/
