# Overlay of benchmark_base.toml: all sweeps and maps on the CPU backend.
base_config = "benchmark_base.toml"

[hardware]
# gpu_backend: one of: "none" (CPU) | "auto" | "cuda" | "amdgpu" | "metal" | "oneapi"
gpu_backend = "none"
